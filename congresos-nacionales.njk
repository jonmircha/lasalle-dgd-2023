---
layout: layout_page.njk
title: Congresos Nacionales e Internacionales
description: null
ogImg: null
url: congresos-nacionales
---

{% include "header.njk" %}

{% include "menu_carpeta_11.njk" %}

<main id="main" class="main">
  <div class="container">
    <h2 class="display-6 my-5 p-3 text-center fw-bold">{{ title }}</h2>
    <section class="row my-5 p-3">
      <article class="col-10 offset-1 fs-5">
        <p>
          <b>Descripción:</b> Los viajes que se realzan de manera recurrente son al Congreso Internacional Dejando Huella que se lleva a cabo en la ciudad de Querétaro y el Congreso Internacional DesignFest que se realiza en Guadalajara Jalisco. 
        </p>
        <p>
          En los viajes internacioneales se busca el acercamiento a los estudiantes con agencias de diseño y visitas a museos. 
        </p>
      </article>
    </section>
    <section class="row g-0 my-lightbox-toggle">
      <h2 class="display-5 mt-5 p-3 text-center fw-bold">Querétaro</h2>
      {% for i in range(1, 12) %}
        {% set col = 6 %}
        {% if i % 3 == 0 %}
          {% set col = 12 %}
        {% endif %}
        <a href="/img/11/qro_{{i}}.jpg" data-toggle="lightbox" data-gallery="example-gallery" class="col-6 col-md-{{col}}">
          <img src="/img/11/qro_{{i}}.jpg" class="img-fluid w-100">
        </a>
      {% endfor %}
    </section>
    <section class="row g-0 my-lightbox-toggle">
      <h2 class="display-5 mt-5 p-3 text-center fw-bold">Guadalajara</h2>
      {% for i in range(1, 16) %}
        {% set col = 6 %}
        {% if i % 3 == 0 %}
          {% set col = 12 %}
        {% endif %}
        <a href="/img/11/design_fest_{{i}}.jpg" data-toggle="lightbox" data-gallery="example-gallery" class="col-6 col-md-{{col}}">
          <img src="/img/11/design_fest_{{i}}.jpg" class="img-fluid w-100">
        </a>
      {% endfor %}
    </section>
    <section class="row g-0 my-lightbox-toggle">
      <h2 class="display-5 mt-5 p-3 text-center fw-bold">Nueva York</h2>
      {% for i in range(1, 15) %}
        {% set col = 6 %}
        {% if i % 3 == 0 %}
          {% set col = 12 %}
        {% endif %}
        <a href="/img/11/ny_{{i}}.jpg" data-toggle="lightbox" data-gallery="example-gallery" class="col-6 col-md-{{col}}">
          <img src="/img/11/ny_{{i}}.jpg" class="img-fluid w-100">
        </a>
      {% endfor %}
    </section>
    <section class="row g-0 my-lightbox-toggle">
      <h2 class="display-5 mt-5 p-3 text-center fw-bold">San Francisco</h2>
      {% for i in range(1, 22) %}
        {% set col = 6 %}
        {% if i % 3 == 0 %}
          {% set col = 12 %}
        {% endif %}
        <a href="/img/11/san_fran_{{i}}.jpg" data-toggle="lightbox" data-gallery="example-gallery" class="col-6 col-md-{{col}}">
          <img src="/img/11/san_fran_{{i}}.jpg" class="img-fluid w-100">
        </a>
      {% endfor %}
    </section>
  </div>
</main>